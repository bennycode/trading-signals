{
  "name": "trading-signals-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
