{
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@tstv/eslint-config": "^4.1.1",
    "@types/node": "^24.10.4",
    "@vitest/coverage-v8": "^4.0.16",
    "cross-env": "^10.1.0",
    "dependency-cruiser": "^17.3.5",
    "eslint": "^9.39.2",
    "lefthook": "^2.0.13",
    "lerna": "^9.0.3",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "name": "typedtrader",
  "private": true,
  "scripts": {
    "clean": "lerna run clean --parallel",
    "dist": "lerna run dist",
    "docs": "lerna run dist --scope trading-signals && lerna run dev --scope trading-signals-docs",
    "fix": "lerna run fix --parallel",
    "graph": "nx graph",
    "lint": "lerna run lint --parallel",
    "release": "npm install && npm run dist && lerna publish && npm run clean",
    "test": "lerna run --no-sort --concurrency 8 test",
    "test:types": "lerna run --no-sort --concurrency 8 test:types"
  },
  "version": "0.0.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
