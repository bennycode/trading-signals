---
title: 'Backtest'
description: 'Run trading strategies against historical candle data'
content:
  width: full
---

import {BacktestApp} from '../components/BacktestApp';

# Backtesting

Configure a trading strategy, pick a market regime, and run it against historical candle data — entirely in your browser. Every run also executes a buy-and-hold baseline so you can see whether the strategy actually beats doing nothing.

<BacktestApp />
