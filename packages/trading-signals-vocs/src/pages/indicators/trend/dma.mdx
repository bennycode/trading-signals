---
title: 'DMA'
description: 'Dual Moving Average'
---

import {IndicatorPageDemo} from '../../../components/IndicatorPageDemo';

# Dual Moving Average (DMA)

Compares two moving averages. When the short MA crosses above the long MA, it signals a potential buy opportunity.

## Live Demo

Pick a market regime to see how DMA(9) reacts:

<IndicatorPageDemo category="trend" id="dma" />

## Usage

```ts twoslash
import {DMA, SMA} from 'trading-signals';

// DMA yields results once 9 inputs have been added
const dma = new DMA(5, 9, SMA);
```
