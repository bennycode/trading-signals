{
  "exclude": [
    "${configDir}/dist",
    "${configDir}/docs",
    "${configDir}/node_modules",
    "${configDir}/src/fixtures",
    "${configDir}/src/start",
    "${configDir}/src/start.ts",
    "${configDir}/src/**/demo",
    "${configDir}/*.config.ts",
    "**/*.test.ts"
  ],
  "extends": "./tsconfig.lib.json"
}
